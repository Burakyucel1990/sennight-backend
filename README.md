# Sennight Backend (NodeJS + Express)

Sade ve çalışır bir başlangıç API'si. Veriler demo olarak `data/*.json` dosyalarına yazılır.

## Kurulum

```bash
npm install
cp .env.example .env
npm run dev
